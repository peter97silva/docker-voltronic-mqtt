name: voltronic-mqtt
services:
    python:
        device_cgroup_rules:
            - c 188:* rmw
        stdin_open: true
        tty: true
        container_name: voltronic-mqtt
platform: linux/arm64/v8
volumes:
  pgdata: {}
